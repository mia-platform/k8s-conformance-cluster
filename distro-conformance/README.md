# `vab` configuration file - K8s Conformance

This directory contains the `vab` configuration with all the modules and addons needed to run K8s Conformance tests.

Include it in the `vab` project folder before syncing the resources.
